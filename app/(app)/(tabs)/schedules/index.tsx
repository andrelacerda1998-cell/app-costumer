/**
 * Agenda — a lista dos serviços marcados, agora com separador próprio.
 *
 * O ecrã já existia em (pages)/(schedules); aqui é só o ponto de entrada da
 * barra de baixo, para não haver duas cópias da mesma lista a divergir.
 */
export { default } from "@/app/(app)/(pages)/(schedules)/[schedule]";
